# QA Automation Showcase Projects

These projects demonstrate maintainable API, Robot Framework, and Playwright test automation patterns. Each directory is deliberately self-contained so it can become an independent public repository.

Before publishing, update the repository URL, author details, and any demo URLs to match the chosen project scope.
